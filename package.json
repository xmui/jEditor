{
  "name": "jeditor",
  "version": "1.4.0",
  "private": true,
  "description": "Minimalist local photo viewer with instant lossless rotation and cropping",
  "scripts": {
    "start": "node scripts/serve.js",
    "build": "node scripts/build-standalone.js",
    "test": "node test/run.js",
    "bump": "node scripts/bump-version.js"
  },
  "devDependencies": {
    "playwright-core": "^1.45.0"
  }
}
